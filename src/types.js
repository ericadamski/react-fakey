const paragraph = 'p';
const header = 'h';
const table = 't';
const image = 'img';
const input = 'i';
const textarea = 'text';
const list = 'list';
const columns = 'cols';

export default {
  paragraph,
  p: paragraph,
  header,
  h: header,
  table,
  t: table,
  image,
  img: image,
  input,
  i: input,
  textarea,
  list,
  columns,
};
